# Product

## Register

product

## Users

使用 DeepSeek Harness Web 的开发者和高级用户。他们在长对话、工具调用和子代理协作中阅读工作过程，需要快速区分过程与最终答案，同时保留随时展开查看细节的能力。

## Product Purpose

dsh-fold-it-up 是 DSH Web 的界面插件，强制把已经结束轮次的工作过程收成一行，并保留答案、错误提示和上下文内容的可读性。成功标准是折叠行为可靠、展开行为完整、视觉上与 DSH 原生控件一致，并且不影响侧边栏、子代理和页面交互。

## Brand Personality

原生、克制、可靠。插件应当像 DSH 自己提供的功能，减少视觉和行为上的意外。

## Anti-references

不要使用与 DSH 原版不同的折叠控件样式，不要用自定义视觉装饰替代产品组件，不要自动移动用户正在阅读的视图，不要因侧边栏或子代理状态变化造成循环更新、卡死或持续报错。

## Design Principles

- 优先复用 DSH 的真实组件、样式和交互契约。
- 工作过程可收起，但答案和必要的结束信息始终可读。
- 历史加载、会话切换、侧边栏和子代理都必须与主对话同样稳定。
- 状态变化应当收敛到稳定结果，不能由界面重排触发更新风暴。

## Accessibility & Inclusion

保留键盘可操作性、可见焦点、浏览器查找展开隐藏内容和减少动效支持；不以自动滚动打断用户主动阅读或浏览。
